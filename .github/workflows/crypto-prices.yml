name: Crypto Prices CI

on:
  push:
    branches: [ main ]
    paths:
      - 'services/crypto-prices/**'
      - '.github/workflows/crypto-prices.yml'
      - 'docker-compose.yml'
  pull_request:
    paths:
      - 'services/crypto-prices/**'
      - '.github/workflows/crypto-prices.yml'
      - 'docker-compose.yml'
  workflow_dispatch:

concurrency:
  group: crypto-prices-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/crypto-prices
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: services/crypto-prices/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest

      - name: Run unit tests
        run: pytest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: services/crypto-prices
          load: true
          push: false
          tags: crypto-prices:ci
.github/workflows/stocks-prices.yml (line 1)

yaml

name: Stocks Prices CI

on:
  push:
    branches: [ main ]
    paths:
      - 'services/stocks-prices/**'
      - '.github/workflows/stocks-prices.yml'
      - 'docker-compose.yml'
  pull_request:
    paths:
      - 'services/stocks-prices/**'
      - '.github/workflows/stocks-prices.yml'
      - 'docker-compose.yml'
  workflow_dispatch:

concurrency:
  group: stocks-prices-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/stocks-prices
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: services/stocks-prices/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest

      - name: Run unit tests
        run: pytest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: services/stocks-prices
          load: true
          push: false
          tags: stocks-prices:ci
          